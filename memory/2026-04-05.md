# 2026-04-05 Daily Memory

## Archer API Investigation Session

### 问题
用户要求使用 Archer `/link_data` 接口获取每个商品的佣金数据来计算 ROI。

### 发现的问题
1. `/get_all_links` API 返回75个商品，**不包含佣金字段**（没有 commission_amount）
2. `/link_data` API 返回所有佣金为 $0.00
3. 测试 ASIN B07RKW5H68 在 `get_all_links` 中找不到，但在 `get_single_product` 中存在且状态为 available

### API 测试结果
- `/get_single_product?asin=B07RKW5H68` - 商品存在，状态 available: True
- `/link_data` - 返回73条链接数据，但所有 commission_amount 都是 0
- 环境中 requests 库网络超时，无法完整测试

### 用户的商品数据
- B07RKW5H68 - 用户确认后台有效，有佣金
- 用户要求查看 B07RKW5H68 的完整 API 返回数据

### 待确认
- [ ] 用户提供的 Archer 后台截图/数据
- [ ] 为什么 link_data 所有佣金都是0
- [ ] 正确的佣金数据获取方式

### Archer API 端点
- Base URL: https://api.archeraffiliates.com
- Auth: OAuth2 `/token`
- 商品: `/get_all_links`, `/get_single_product?asin=`
- 订单: `/link_data` (参数: startdate, enddate, sort_order)
